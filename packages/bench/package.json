{
  "name": "@amojs/bench",
  "version": "0.6.0-dev",
  "private": true,
  "type": "module",
  "description": "AmoJS micro-benchmarks — never published. Competitor libraries live here so the runtime keeps zero dependencies.",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@amojs/core": "workspace:*",
    "@vue/reactivity": "3.6.0-rc.2",
    "alien-signals": "^3.2.0",
    "solid-js": "^1.9.0"
  }
}
